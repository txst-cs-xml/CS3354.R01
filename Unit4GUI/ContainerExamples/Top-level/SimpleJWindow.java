import javax.swing.JWindow;

public class SimpleJWindow {
    public static void main(String[] args) {
        JWindow window = new JWindow(); // Create a window
        window.setSize(300, 200); // Set the size of the window
        window.setVisible(true); // Make the window visible
    }
}
